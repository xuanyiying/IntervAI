import React from 'react';
import type { CreationProps } from '../Creation';
export interface CreationLabelProps {
    shortcutKeysIcon?: string[];
    prefixCls: string;
}
declare const useCreation: ({ icon, label, align, shortcutKeyInfo, prefixCls, }: Pick<CreationProps, 'label' | 'icon' | 'align' | 'shortcutKeyInfo' | 'prefixCls'>) => [
    React.ReactNode,
    React.ReactNode,
    CreationProps['align']
];
export default useCreation;
